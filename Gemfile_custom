# Overrides and adds customized gems in this file
# Read more on documentation:
# * English: https://github.com/consul/consul/blob/master/CUSTOMIZE_EN.md#gemfile
# * Spanish: https://github.com/consul/consul/blob/master/CUSTOMIZE_ES.md#gemfile

gem "omniauth_openid_connect", "~> 0.3.5"

group :development do
  gem 'rack-mini-profiler'
  gem 'solargraph'
  # gem 'prosopite'
  # gem 'pg_query'

  gem 'foreman', '~> 0.90.0'
end

gem 'httparty'
gem 'sd_notify', '~> 0.1.1'

gem 'ruby-saml', '~> 1.16'

gem 'geocoder', '~> 1.8', '>= 1.8.3'
gem 'rgeo', '~> 3.0', '>= 3.0.1'
gem 'rgeo-geojson', '~> 2.2'

gem "sentry-ruby"
gem "sentry-rails"

gem "acts_as_list"
gem "jwt"
gem 'rack-cors'

gem "prawn", "2.5.0"
gem 'prawn-svg', '~> 0.37.0'
gem "rqrcode", "~> 2.0"

# Adm
gem 'jsbundling-rails', '~> 1.3', '>= 1.3.1'
gem 'cssbundling-rails', '~> 1.4', '>= 1.4.3'

gem 'turbo-rails'

gem 'pundit', '~> 2.5', '>= 2.5.1'
gem 'pagy', '~> 9.4'
# /Adm

gem 'liquid'

gem 'ruby_llm', '1.16.0'

gem 'pdf-reader', '2.15.1'

gem 'docx', '0.10.0'

# rubyzip is a ruby module for reading and writing zip files
# Already resolved transitively through docx; declared explicitly because
# DocumentImageExtractor uses Zip::File directly. Held at 2.3.2 rather than the
# latest 3.4.1 because docx and selenium-webdriver both require rubyzip < 3.
gem 'rubyzip', '2.3.2'

group :test do
  gem 'shoulda-matchers', '~> 6.0'
end

group :development, :test do
  gem 'pry', '~> 0.14'
  gem 'pry-byebug', '~> 3.9'
  gem 'pry-rails'

  # Improves pry shortcuts
  gem 'rb-readline', '0.5.5'

  gem 'rswag-specs'
end

gem 'proj4rb'

# A Ruby gem to transform HTML into PDF, PNG or JPEG by wrapping the NodeJS Google Puppeteer driver for Chromium
gem 'grover', '1.2.10'
