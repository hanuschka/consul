<article
  class="files-asset-card"
  data-id="<%= asset.id %>"
  data-type="<%= type %>"
  data-title="<%= asset.title %>"
  data-description="<%= asset.description %>"
  data-alt-text="<%= asset.alt_text %>"
  data-filename="<%= filename %>"
>
  <div class="files-asset-card--thumb">
    <% if detail_url.present? %>
      <a
        href="<%= detail_url %>"
        class="files-asset-card--thumb-link"
        data-turbo-frame="files-asset-detail"
        data-action="click->files--asset-modal#open"
        aria-label="<%= t("files.show.view_details") %>"
      ></a>
    <% end %>

    <% if image? %>
      <%= render Shared::ImageWithLightboxComponent.new(
        image_url: asset.custom_thumb_url(width: 600, height: 450),
        original_url: asset.url_content,
        alt: asset.alt_text.presence || filename,
        gallery: "files-#{type}"
      ) %>
    <% elsif has_preview? %>
      <img
        src="<%= asset.custom_thumb_url(width: 600, height: 450) %>"
        alt="<%= asset.alt_text.presence || filename %>"
        class="files-asset-card--thumb-image"
        loading="lazy"
      >
    <% else %>
      <i class="fa <%= icon_class %> files-asset-card--thumb-icon"></i>
    <% end %>

    <div class="files-asset-card--badges">
      <rich-tooltip>
        <span
          class="files-asset-card--admin-badge"
          aria-label="<%= t("files.card.admin_badge") %>"
        >
          <i class="fa fa-shield-alt"></i>
        </span>
        <template><%= t("files.card.admin_badge") %></template>
      </rich-tooltip>

      <% if picture? %>
        <rich-tooltip
          trigger-only
          hover-only
        >
          <inline-popup
            class="files-asset-card--alt-popup"
            placement="top"
            align="start"
          >
            <button
              type="button"
              class="files-asset-card--alt-badge js-files-alt-badge"
              data-alt-state="<%= alt_text.present? ? "set" : "missing" %>"
              aria-label="<%= t("files.card.edit_alt_text") %>"
            >
              <i class="fa fa-comment-dots files-asset-card--alt-badge-icon -set" aria-hidden="true"></i>
              <i class="fa fa-exclamation-triangle files-asset-card--alt-badge-icon -missing" aria-hidden="true"></i>
            </button>

            <template>
              <div class="files-alt-edit">
                <div class="files-alt-edit--intro">
                  <i class="fa fa-universal-access files-alt-edit--intro-icon" aria-hidden="true"></i>
                  <p class="files-alt-edit--intro-text"><%= t("files.card.alt_edit.description") %></p>
                </div>

                <label class="files-alt-edit--label"><%= t("files.card.alt_text") %></label>
                <textarea
                  class="files-alt-edit--input js-files-alt-input"
                  rows="3"
                  autofocus
                  placeholder="<%= t("files.card.alt_edit.placeholder") %>"
                ><%= alt_text %></textarea>

                <div class="files-alt-edit--actions">
                  <button
                    type="button"
                    class="files-alt-edit--cancel"
                    data-inline-popup-close
                  >
                    <%= t("files.card.alt_edit.cancel") %>
                  </button>

                  <button
                    type="button"
                    class="files-alt-edit--save js-files-alt-save"
                    data-action="click->files--filter-bar#altPopupSave"
                  >
                    <span class="material-symbols-outlined files-alt-edit--save-icon" aria-hidden="true">save</span>
                    <i class="fa fa-floppy-disk files-alt-edit--save-fa-icon" aria-hidden="true"></i>
                    <%= t("files.card.alt_edit.save") %>
                  </button>
                </div>
              </div>
            </template>
          </inline-popup>

          <template><%= t("files.card.alt_badge_tooltip") %></template>
        </rich-tooltip>
      <% end %>
    </div>

    <div class="files-asset-card--actions">
      <button
        type="button"
        class="files-asset-card--icon-button js-files-edit-asset"
        data-action="click->files--filter-bar#editClicked"
        title="<%= t("files.card.edit") %>"
        aria-label="<%= t("files.card.edit") %>"
      >
        <i class="fa fa-pen"></i>
      </button>

      <button
        type="button"
        class="files-asset-card--icon-button -danger js-files-delete-asset"
        data-action="click->files--filter-bar#deleteClicked"
        data-confirm-message="<%= t("files.card.delete_confirm") %>"
        data-delete-url="<%= delete_url %>"
        title="<%= t("files.card.delete") %>"
        aria-label="<%= t("files.card.delete") %>"
      >
        <i class="fa fa-trash"></i>
      </button>
    </div>

    <% if image? %>
      <a
        href="<%= asset.url_content %>"
        download="<%= filename %>"
        class="files-asset-card--icon-button -bottom-right"
        title="<%= t("files.card.download") %>"
        aria-label="<%= t("files.card.download") %>"
      >
        <i class="fa fa-download"></i>
      </a>
    <% end %>
  </div>

  <div class="files-asset-card--meta">
    <p
      class="files-asset-card--filename"
      title="<%= filename %>"
    >
      <%= filename %>
    </p>

    <dl class="files-asset-card--details">
      <% if projekt_name.present? %>
        <div class="files-asset-card--detail-row">
          <dt><%= t("files.card.projekt") %></dt>
          <dd>
            <% if projekt_url.present? %>
              <%= link_to projekt_name, projekt_url, target: "_blank", rel: "noopener", class: "files-asset-card--resource-link" %>
            <% else %>
              <%= projekt_name %>
            <% end %>
          </dd>
        </div>
      <% end %>

      <% if humanized_size.present? %>
        <div class="files-asset-card--detail-row">
          <dt><%= t("files.card.file_size") %></dt>
          <dd><%= humanized_size %></dd>
        </div>
      <% end %>

      <% if dimensions.present? %>
        <div class="files-asset-card--detail-row">
          <dt><%= t("files.card.dimensions") %></dt>
          <dd><%= dimensions %></dd>
        </div>
      <% end %>

      <% if picture? %>
        <div
          class="files-asset-card--detail-row js-files-alt-text-row"
          <%= "hidden" if alt_text.blank? %>
        >
          <dt><%= t("files.card.alt_text") %></dt>
          <dd class="js-files-alt-text-value"><%= alt_text %></dd>
        </div>
      <% end %>

      <div class="files-asset-card--detail-row">
        <dt><%= t("files.card.uploaded") %></dt>
        <dd><%= created_at_formatted %></dd>
      </div>

      <div class="files-asset-card--detail-row">
        <dt><%= t("files.card.last_edited") %></dt>
        <dd><%= updated_at_formatted %></dd>
      </div>

      <% if uploaded_by.present? %>
        <div class="files-asset-card--detail-row">
          <dt><%= t("files.card.uploaded_by") %></dt>
          <dd><%= render Files::UploaderLinkComponent.new(user: uploaded_by) %></dd>
        </div>
      <% end %>
    </dl>

    <% if type == "document" %>
      <div class="files-asset-card--footer">
        <a
          href="<%= asset.url_content %>"
          target="_blank"
          rel="noopener"
          class="files-asset-card--footer-button"
        >
          <i class="fa fa-arrow-up-right-from-square"></i>
          <%= t("files.card.open_in_new_tab") %>
        </a>

        <a
          href="<%= asset.url_content %>"
          download="<%= filename %>"
          class="files-asset-card--footer-button"
        >
          <i class="fa fa-download"></i>
          <%= t("files.card.download") %>
        </a>
      </div>
    <% end %>
  </div>
</article>
