<nav class="margin-bottom projekt-footer-navigation">
  <div class="row">
    <div class="small-12 column">
      <div style='display:flex;justify-content:end;align-items:end;'>
        <% if show_arrows? %>
          <div class='arrow-controls'>
            <button type="button" id='left-arrow-control' class='js-left-arrow-control disabled'>
              <i class="fas fa-chevron-circle-left"></i>
            </button>
            <button type="button" id='right-arrow-control' class='js-right-arrow-control'>
              <i class="fas fa-chevron-circle-right"></i>
            </button>
          </div>
        <% end %>
      </div>

      <div class='footer-phases-filter-subnav'>
        <% if helpers.embedded? %>
          <button data-url="<%= admin_frame_new_phase_selector_url(projekt_id: @projekt.id) %>" class="page-subnav-tab projekt-footer-phase-tab projekt-footer-phase-tab--add-new js-frame-open-page" title="Neue Phase hinzufügen">
            <i class="fas fa-plus"></i>
          </button>
        <% end %>
        <ul id='filter-subnav' class='key-dates'>
          <% projekt_phases.each do |projekt_phase| %>
            <%= render Pages::Projekts::FooterPhaseTabComponent.new(projekt_phase, default_phase_name) %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
