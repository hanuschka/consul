<%= render Shared::ResourcesList::Component.new(title: @title, wide: @wide) do |c| %>
  <% c.body do %>
    <% @proposals.each do |proposal| %>
      <%= render Proposals::ProposalListItem::Component.new(proposal: proposal, wide: @wide) %>
    <% end %>
  <% end %>
<% end %>
