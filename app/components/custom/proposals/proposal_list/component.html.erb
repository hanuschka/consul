<%= render Shared::ResourcesList::Component.new(title: @title, wide: @wide) do |c| %>
  <% c.body do %>
    <% @projekts.each do |projekt| %>
      <%= render Proposals::ProposalListItem::Component.new(projekt: projekt, wide: @wide) %>
    <% end %>
  <% end %>
<% end %>
