<%= link_list_sdg_goals(*links, class: "sdg-#{parameter_name}-tag-list") %>

<% if selected_goals.any? %>
  <div class="filter margin-top">
    <label for="advanced_search_target"><%= sanitize(t("custom.sdg.targets.label",
                                                       more_link: link_to( t('custom.sdg.targets.more_link_text'), sdg_goals_path ))) %></label>
    <%= select_tag("select_target", target_options,
                   include_blank: t("custom.sdg.targets.select"), class: 'js-sdg-custom-target-filter-dropdown')  %>
  </div>
<% end %>
