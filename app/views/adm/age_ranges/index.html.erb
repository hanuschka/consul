<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs) do |header| %>
  <% header.with_hint do %>
    <p class="adm-hint__text"><%= t(".intro_text_p1") %></p>
    <p class="adm-hint__text adm-hint__text--spaced"><%= t(".intro_text_p2") %></p>
  <% end %>
<% end %>

<div class="kern-container my-5">
  <%= new_resource_link(new_adm_age_range_path, t('.new_age_range')) %>
</div>

<%= render Adm::TabsComponent.new(tabs: [
  { label: t(".filters.for_restrictions"), url: adm_age_ranges_path(filter: "for_restrictions"), current: @current_filter == "for_restrictions" },
  { label: t(".filters.for_stats"), url: adm_age_ranges_path(filter: "for_stats"), current: @current_filter == "for_stats" }
]) %>

<div class="kern-container mb-5">
  <% if @age_ranges.any? %>
    <div class="kern-table-responsive" tabindex="0">
      <table class="kern-table">
        <thead class="kern-table__head">
          <tr class="kern-table__row">
            <th scope="col" class="kern-table__header"></th>
            <th scope="col" class="kern-table__header"><%= t('.table.name') %></th>
            <th scope="col" class="kern-table__header"><%= t('.table.range') %></th>
            <th scope="col" class="kern-table__header"><%= t('.table.actions') %></th>
          </tr>
        </thead>

        <tbody class="kern-table__body" data-controller="sortable" data-sortable-url-value="<%= reorder_adm_age_ranges_path %>">
          <% @age_ranges.each do |age_range| %>
            <tr id="<%= dom_id(age_range) %>" class="kern-table__row" data-sortable-id="<%= age_range.id %>">
              <td class="kern-table__cell" data-sortable-handle style="cursor: grab;">
                <span class="material-symbols-outlined">drag_indicator</span>
              </td>
              <td scope="row" class="kern-table__header"><%= age_range.name %></td>
              <td class="kern-table__cell"><%= age_range.range_label %></td>
              <td class="kern-table__cell">
                <%= render Kern::Table::ActionsComponent.new do |actions| %>
                  <% actions.with_action(label: t('.table.edit'), url: edit_adm_age_range_path(age_range), icon: "edit", style: :edit) %>
                  <% actions.with_action(
                    label: t('.table.delete'),
                    url: adm_age_range_path(age_range),
                    icon: "delete", style: :delete,
                    data: { turbo_method: :delete, turbo_confirm: t('.table.delete_confirm') }
                  ) %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="my-5">
      <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
    </div>
  <% else %>
    <%= empty_state(
      title: t(".empty_title"),
      description: t(".empty_description"),
      icon: "calendar_month"
    ) %>
  <% end %>
</div>
