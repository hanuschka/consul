<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: @deficiency_report.title, breadcrumbs: @breadcrumbs) %>

<%= render Adm::TabsComponent.new(tabs: deficiency_report_tabs(@deficiency_report)) %>

<article class="kern-container mb-5">
  <%= turbo_frame_tag "deficiency_report_content" do %>
    <%= form_with model: @deficiency_report, url: adm_deficiency_reports_deficiency_report_path(@deficiency_report),
          method: :patch, multipart: true do |form| %>

      <%= render Kern::FormFieldComponent.new(divider: false, inline: true,
        label: t(".labels.title")) do %>
        <%= form.text_field :title, label: false %>
      <% end %>

      <%= render Kern::FormFieldComponent.new(divider: false, inline: true,
        label: t(".labels.description")) do %>
        <%= form.rich_text_area :description, label: false %>
      <% end %>

      <%= render Kern::FormFieldComponent.new(divider: false, inline: true,
        label: t(".labels.image")) do %>
        <%= render Kern::Form::ImageableImageComponent.new(form: form) %>
      <% end %>

      <%= render Kern::FormFieldComponent.new(divider: false, inline: true,
        label: t(".labels.documents")) do %>
        <%= render Kern::Form::DocumentsComponent.new(form: form) %>
      <% end %>

      <%= render Kern::FormFieldComponent.new(divider: false, inline: true,
        label: t(".labels.video_url")) do %>
        <%= form.text_field :video_url, label: false %>
      <% end %>

      <%= render Kern::FormFieldComponent.new(divider: false, inline: true,
        label: t(".labels.on_behalf_of")) do %>
        <%= form.text_field :on_behalf_of, label: false %>
      <% end %>

      <%= render Kern::FormFieldComponent.new(divider: false, inline: true,
        label: t(".labels.category")) do %>
        <div data-feature-style-mode="color">
          <div data-feature-style-mode="icon">
            <div class="map-feature-pills">
              <% no_category_id = "deficiency_report_deficiency_report_category_id_none" %>
              <input type="radio" name="deficiency_report[deficiency_report_category_id]" value="" id="<%= no_category_id %>"
                style="display:none" <%= "checked" unless @deficiency_report.deficiency_report_category_id.present? %>>
              <label for="<%= no_category_id %>" class="map-feature-pill map-feature-pill--sentiment">
                <%= t(".no_category") %>
              </label>

              <% DeficiencyReport::Category.all.each do |category| %>
                <% input_id = "deficiency_report_deficiency_report_category_id_#{category.id}" %>
                <input type="radio" name="deficiency_report[deficiency_report_category_id]" value="<%= category.id %>" id="<%= input_id %>"
                  style="display:none" <%= "checked" if @deficiency_report.deficiency_report_category_id == category.id %>>
                <label for="<%= input_id %>"
                  class="map-feature-pill map-feature-pill--sentiment"
                  data-color="<%= category.color %>"
                  data-icon-name="<%= category.icon %>"
                  data-icon-unicode="<%= AwesomeIcon.find_by(name: category.icon)&.unicode %>"
                  style="--pill-color:<%= category.color %>;--pill-text-color:<%= pick_text_color(category.color) %>">
                  <% if category.icon.present? %>
                    <i class="fas fa-<%= category.icon %>"></i>
                  <% end %>
                  <%= category.name %>
                </label>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>

      <div class="kern-row">
        <div class="kern-col-10">
          <%= form.fields_for :map_location do |ml_form| %>
            <%= render Kern::MapComponent.new(
              map_location: @deficiency_report.map_location,
              form: ml_form,
              editable: true,
              admin_editor: false,
              height: 500
            ) %>
          <% end %>
        </div>
      </div>

      <div class="d-flex gap-2 mb-4">
        <%= kern_button(text: t("shared.submit"), icon: "save", type: "submit") %>
        <%= kern_link_button(adm_deficiency_reports_deficiency_report_path(@deficiency_report),
              text: t("shared.cancel"), style: :secondary) %>
      </div>
    <% end %>
  <% end %>
</article>
