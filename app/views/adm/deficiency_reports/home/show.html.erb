<%= content_for :title, t("adm.deficiency_reports.home.title") %>

<%= render Adm::HeaderComponent.new(title: t("adm.deficiency_reports.home.title"), breadcrumbs: @breadcrumbs) %>

<%= render Adm::DashboardHomeComponent.new(
  team_members: @team_members,
  team_url: adm_deficiency_reports_officers_path,
  recent_items: @recent_items,
  recent_items_url: adm_deficiency_reports_deficiency_reports_list_path,
  recent_item_columns: [
    { label: t("shared.table.title"), value: ->(item) { item.title }, url: ->(item) { adm_deficiency_reports_deficiency_report_path(item) } },
    { label: t("shared.table.status"), value: ->(item) { item.status&.title || "—" } }
  ],
  intro_text: @section_setting&.intro_text,
  quick_links: @quick_links,
  stats: @stats,
  contact_persons: @contact_persons,
  notice: @section_setting,
  activities: @activities,
  section_settings_path: policy([:adm, SectionSetting]).edit? ? edit_adm_section_setting_path("deficiency_reports") : nil
) %>
