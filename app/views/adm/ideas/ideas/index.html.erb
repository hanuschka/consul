<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs) do |header| %>
  <% header.with_hint do %>
    <p class="adm-hint__text"><%= t(".page_hint") %></p>
  <% end %>
<% end %>

<div class="kern-container my-5">
  <% if @ideas.any? %>
    <div class="d-flex align-items-center justify-content-end mb-3">
      <a href="<%= url_for(request.query_parameters.merge(format: :geojson)) %>" class="adm-text-link" data-turbo="false">
        <span class="material-symbols-outlined" aria-hidden="true">map</span>
        <%= t(".download_geojson") %>
      </a>
    </div>

    <div class="kern-table-responsive" tabindex="0">
      <table class="kern-table">
        <thead class="kern-table__head">
          <tr class="kern-table__row">
            <%= render Kern::Table::HeaderComponent.new(:title, t(".table.title"), **@title_header_options) %>
            <th scope="col" class="kern-table__header"><%= t(".table.admin_accepted") %></th>
            <%= render Kern::Table::HeaderComponent.new(:created_at, t(".table.created_at"), **@created_at_header_options) %>
            <th scope="col" class="kern-table__header"><%= t(".table.author") %></th>
            <th scope="col" class="kern-table__header"><%= t(".table.status") %></th>
            <%= render Kern::Table::HeaderComponent.new(:idea_category_id, t(".table.category"), **@category_header_options) %>
            <%= render Kern::Table::HeaderComponent.new(:idea_officer_id, t(".table.officer"), **@officer_header_options) %>
            <th scope="col" class="kern-table__header"><%= t("shared.table.actions") %></th>
          </tr>
        </thead>

        <tbody class="kern-table__body">
          <% @ideas.each do |idea| %>
            <%= render "idea_row", idea: idea %>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="my-5">
      <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
    </div>
  <% else %>
    <%= empty_state(title: t(".no_items_title"), description: t(".no_items_description"), icon: "lightbulb") %>
  <% end %>
</div>
