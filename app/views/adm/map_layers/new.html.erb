<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs, narrow: true, back_button_url: @back_url) %>

<div class="kern-container kern-container--form my-5">
  <%= render "form" %>
</div>
