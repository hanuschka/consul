<% nested = memoable.is_a?(Memo) %>

<div
  class="<%= "adm-memo__replies" if nested %>"
  id="<%= nested ? dom_id(memoable, :tree) : "memos_tree" %>"
>
  <%= render partial: "adm/memos/memo", collection: memos, as: :memo %>
</div>
