<div class="<%= 'adm-memo__replies' if memoable.is_a?(Memo) %>"
     id="<%= memoable.is_a?(Memo) ? dom_id(memoable, :tree) : 'memos_tree' %>">
  <% memos.each do |memo| %>
    <%= render "adm/memos/memo", memo: memo %>
  <% end %>
</div>
