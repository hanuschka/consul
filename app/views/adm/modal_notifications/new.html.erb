<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs, narrow: true, back_button_url: adm_modal_notifications_path) %>

<div class="kern-container my-5">
  <%= render "form" %>
</div>
