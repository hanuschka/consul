<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs,
                                    back_button_url: adm_municipal_plans_officer_groups_path, narrow: true) %>

<div class="kern-container my-5">
  <%= render "form" %>
</div>
