<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs, back_button_url: @landing_page.present? ? edit_adm_landing_pages_landing_page_path(@landing_page, anchor: "navbar") : adm_navbar_path, narrow: true) %>

<div class="kern-container kern-container--form my-5">
  <%= render 'form' %>
</div>
