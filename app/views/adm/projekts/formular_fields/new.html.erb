<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs, narrow: true, back_button_url: formular_adm_projekts_phase_path(@projekt_phase)) %>

<div class="kern-container kern-container--form my-5">
  <%= render "form", url: adm_projekts_phase_formular_fields_path(@projekt_phase) %>
</div>
