<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs) do |header| %>
  <% header.with_hint do %>
    <p class="adm-hint__text"><%= t(".hint_text") %></p>
  <% end %>
<% end %>

<div class="kern-container">
  <% if Adm::Projekts::ProjektManagerPolicy.new(current_user, nil).create? %>
    <%= new_resource_link(new_adm_projekts_manager_path, t('.new_link')) %>
  <% end %>

  <% if @projekt_managers.any? %>
    <div class="kern-table-responsive" tabindex="0">
      <table class="kern-table">
        <thead class="kern-table__head">
          <tr class="kern-table__row">
            <th scope="col" class="kern-table__header"><%= t("shared.table.id") %></th>
            <th scope="col" class="kern-table__header"><%= t("shared.table.username") %></th>
            <th scope="col" class="kern-table__header"><%= t("shared.table.email") %></th>
            <th scope="col" class="kern-table__header"><%= t(".manage_all_projekts") %></th>
            <th scope="col" class="kern-table__header"><%= t("shared.table.actions") %></th>
          </tr>
        </thead>
        <tbody class="kern-table__body">
          <% @projekt_managers.each do |projekt_manager| %>
            <%= render "projekt_manager", projekt_manager: projekt_manager %>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="my-5">
      <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
    </div>
  <% else %>
    <%= empty_state(title: t(".no_projekt_managers_title"), description: t(".no_projekt_managers_description"), icon: "supervisor_account") %>
  <% end %>

  <%= render partial: "adm/role_assignments/pending_role_assignments", locals: { pending_role_assignments: @pending_role_assignments } %>
</div>
