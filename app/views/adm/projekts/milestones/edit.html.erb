<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs, narrow: true) %>

<div class="kern-container kern-container--form my-5">
  <%= render "adm/projekts/milestones/form", url: milestone_update_url(@milestone) %>
</div>
