<%= turbo_stream.update dom_id(@projekt_phase, :active) do %>
  <%= render Adm::AttributeEditors::BooleanComponent.new(
    @projekt_phase,
    :active,
    path: toggle_active_adm_projekts_phase_path(@projekt_phase)
  ) %>
<% end %>
