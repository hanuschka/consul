<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs, back_button_url: progress_bars_index_url, narrow: true) %>

<div class="kern-container kern-container--form my-5">
  <%= render "adm/projekts/progress_bars/form", url: progress_bars_create_url %>
</div>
