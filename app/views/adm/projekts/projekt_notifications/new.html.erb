<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs) %>

<div class="kern-container my-5">
  <%= render "form", url: adm_projekts_phase_projekt_notifications_path(@projekt_phase) %>
</div>
