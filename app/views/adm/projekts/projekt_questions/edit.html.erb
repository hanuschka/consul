<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs, back_button_url: projekt_questions_adm_projekts_phase_path(@projekt_phase), narrow: true) %>

<div class="kern-container my-5">
  <%= render "form", url: adm_projekts_phase_projekt_question_path(@projekt_phase, @projekt_question) %>
</div>
