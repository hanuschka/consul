<%= content_for :title, @page.title %>

<%= render Adm::HeaderComponent.new(title: @page.title, breadcrumbs: @breadcrumbs, narrow: true) %>

<div class="kern-container my-5">
  <%= render Adm::AttributeEditorComponent.new(@page, :title, :string, inline: true, divider: false) %>
  <%= render Adm::AttributeEditorComponent.new(@page, :content, :rich_text, wide: true) %>
</div>
