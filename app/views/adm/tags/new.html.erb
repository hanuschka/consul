<%= content_for :title, t(".title") %>

<%= render Adm::HeaderComponent.new(title: t(".title"), breadcrumbs: @breadcrumbs, back_button_url: adm_tags_path, narrow: true) %>

<div class="kern-container kern-container--form my-5">
  <%= render "form" %>
</div>
