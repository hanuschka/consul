<% provide :title do %>
  <%= t("admin.header.title") %> - <%= t("admin.menu.site_customization.pages") %> - <%= t("admin.site_customization.pages.new.title") %>
<% end %>

<% if @page.landing? %>
  <%= back_link_to admin_site_customization_landing_pages_path %>
<% else %>
  <%= back_link_to admin_site_customization_pages_path %>
<% end %>

<div class="row">
  <div class="small-12 column">
    <% if @page.landing? %>
      <h2><%= t("custom.admin.site_customization.landing_pages.new.title") %></h2>
    <% else %>
      <h2><%= t("admin.site_customization.pages.new.title") %></h2>
    <% end %>
  </div>
</div>

<%= render "admin/site_customization/pages/form" %>
