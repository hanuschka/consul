<% if @assets.any? %>
  <%= render collection: @assets, partial: "ckeditor/assets/item", as: :asset %>
<% else %>
  <div class="file-upload-manager-dialog--loading">Keine Bilder gefunden.</div>
<% end %>
