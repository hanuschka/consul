<div id="<%= dom_id(letter) %>" class="formular-follow-up-letter margin-top margin-bottom">
  <h2>Wie möchten Sie Ihr Formularfeld gestalten?</h2>
  <div class="margin-top">
    <%= form_with model: letter, url: polymorphic_path([@namespace, @projekt_phase, @formular, letter]), local: false do |f| %>
      <%= f.text_field :subject %>
      <%= f.text_area :body,
            value: AdminWYSIWYGSanitizer.new.sanitize(letter.body),
            class: "html-area #{ck_editor_class(current_user)}" %>
      <%= f.check_box :show_follow_up_button %>
      <%= f.submit "Speichern" %>
    <% end %>
  </div>
</div>
