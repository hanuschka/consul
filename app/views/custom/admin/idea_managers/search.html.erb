<h2><%= t("custom.admin.idea_managers.search.title") %></h2>

<%= render "admin/shared/user_search", url: search_admin_idea_managers_path %>

<div id="idea_managers">
  <% if @users.any? %>
    <h3><%= page_entries_info @users %></h3>

    <table id="idea_managers">
      <thead>
        <th scope="col"><%= IdeaManager.human_attribute_name(:name) %></th>
        <th scope="col" class="small-6"><%= IdeaManager.human_attribute_name(:email) %></th>
        <th scope="col"><%= t("admin.shared.actions") %></th>
      </thead>
      <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td>
            <%= render Admin::Roles::TableActionsComponent.new(user.idea_manager || user.build_idea_manager) %>
          </td>
        </tr>
      <% end %>
    </table>
  <% else %>
    <div class="callout alert margin">
      <%= t("admin.shared.no_search_results") %>
    </div>
  <% end %>
</div>
