<%= render "custom/admin/projekt_phases/heading" %>

<% unless embedded? %>
  <%= render "custom/admin/projekt_phases/nav_bar" %>
<% end %>

<div class="row">
  <div class="column">
    <%= render Shared::MapComponent.new(
      mappable: @projekt_phase,
      features: @map_coordinates,
      process: 'point-of-interest-map'
    ) %>
  </div>
</div>
