<% unless embedded? %>
  <%= render "custom/admin/projekt_phases/heading" %>
  <%= render "custom/admin/projekt_phases/nav_bar" %>
<% end %>

<div class="row">
  <div class="column">
    <%= render "admin/milestones/milestones", milestoneable: @projekt_phase %>
  </div>
</div>

<% if embedded? %>
  <%= content_for :admin_bottom_navigation do %>
    <%= render Admin::ProjektPhases::FrameNavigationComponent.new(projekt_phase: @projekt_phase, use_link_for_next: true) %>
  <% end %>
<% end %>
