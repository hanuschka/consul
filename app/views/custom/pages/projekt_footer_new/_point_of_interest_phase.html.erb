<div class="projekt-footer--inner">
<%= render "custom/pages/projekt_footer/partials/phase_intro", projekt_phase: @projekt_phase %>

<div class="flex wrap gap-10 margin-bottom items-center">
  <div class="new-resource-cta">
    <%= render Shared::NewButtonComponent.new(projekt_phase: @projekt_phase) %>
  </div>

  <div class="small-card">
    <div class="flex items-center">
      <span class="font-bold">
        <%= t("custom.deficiency_reports.form.category.title") %>:
      </span>
      <div class="margin-left">
        <%= render ProjektPointOfInterestPins::CategoryFilterComponent.new(projekt_phase_id: @projekt_phase.id) %>
      </div>
    </div>
  </div>

</div>
<div class="resources-list--map">
  <%= render Shared::MapComponent.new(
    mappable: @projekt_phase,
    features: @pin_coordinates,
    process: 'point_of_interest_pins',
    map_data_url: map_data_path(
      source: "point_of_interest_phase",
      projekt_phase_id: @projekt_phase.id,
      category_ids: params[:category_ids]
    )
  ) %>
</div>
<div class="flex-layout">
  <div class="main-column">

  </div>
</div>
</div>
