$("#projekt-event-registration-<%= @projekt_event.id %>")
  .replaceWith("<%= j render 'custom/projekt_event_registrations/registration_section', projekt_event: @projekt_event, guest_registered_event_id: @guest_registered_event_id %>");
