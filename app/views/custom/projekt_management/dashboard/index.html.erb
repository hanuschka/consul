<div class="dashboard">
  <h2><%= t("projekt_management.dashboard.index.title") %></h2>

  <p><%= t("projekt_management.dashboard.index.info", org: Setting["org_name"]) %></p>

  <% if ApiClient.registered.where(name: "DT").any? %>
    <%= render ApiClients::ConnectToClientComponent.new %>
  <% end %>
</div>
