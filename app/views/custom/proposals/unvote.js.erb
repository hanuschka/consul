$("#<%= dom_id(@proposal) %>_votes").html("<%= j render(Proposals::NewVotesComponent.new(@proposal)) %>");
$("#<%= dom_id(@proposal) %>_votes_count").html("<%= j t("proposals.proposal.supports", count: @proposal.total_votes) %>");

// $("div[data-voted-badge=<%= @proposal.id %>]").addClass('hide');

// App.Followable.update("<%= dom_id(@follow.followable) %>",
//                       "<%= j render("follows/follow_button", follow: @follow) %>",
//                       "<%= j render("layouts/flash") %>")
