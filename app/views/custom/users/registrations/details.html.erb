<% provide :title do %><%= t("devise_views.users.registrations.new.title") %><% end %>

<h2>Registrieren</h2>

<h4>Information</h4>

<p>Bitte geben Sie Ihren Vor- und Nachnamen und weitere persönliche Daten an. So wird sichergestellt, dass bei einzelnen Themen und Abstimmungen keine unbefugten Personen teilnehmen können. Die Verifizierung kann bis zu fünf Minuten dauern. Nach Abschluss der Verifizierung erhalten Sie von uns eine Email mit der Bestätigung Ihrer Anmeldung oder dem Hinweis auf etwaige Fehler.</p>


<%= form_for(@user, url: update_user_details_path(@user)) do |f| %>
  <%= render "shared/errors", resource: @user %>

  <div class="row">
    <div class="small-12 column">

      <%= f.text_field :first_name, placeholder: t("devise_views.users.registrations.new.first_name_label")  %>

      <%= f.text_field :last_name, placeholder: t("devise_views.users.registrations.new.last_name_label")  %>

      <%= f.text_field :plz, placeholder: t("devise_views.users.registrations.new.plz_label")  %>

      <%= f.date_select :date_of_birth,
                        prompt: true,                   
                        start_year: 1900, end_year: minimum_required_age.years.ago.year %>

      <%= f.text_field :plz, placeholder: t("devise_views.users.registrations.new.plz_label")  %>

      <%= f.select :document_type, document_types, prompt: "" %>

      <%= f.text_field :document_number, placeholder: t("devise_views.users.registrations.new.plz_label")  %>






      <div class="small-12 medium-6 small-centered margin-top">
        <%= f.submit t("devise_views.users.registrations.new.submit"), class: "button expanded" %>
      </div>
    </div>
  </div>
<% end %>

<%= render "devise/shared/links" %>
