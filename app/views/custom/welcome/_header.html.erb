<% if header.present? %>
  <%header_large = Widget::Card.header.find_by title: "header_large" %>
  <style>
    <%if header_large%>
      .home-page .jumbo{
        background-image: url("<%= rails_blob_url(header_large.image.attachment) %>");
        background-size: cover;
        background-position: center 70%;
      }
    <%end%>
  </style>

  <div class="jumbo not-print">
  </div>
<% end %>
