<% if header.present? %>
  <%header_large = Widget::Card.header.find_by title: "header_large" %>
  <style>
    <%if header_large%>
      .home-page .jumbo{
        background-image: linear-gradient(0deg, 
                            rgba(34, 34, 34, 0.85) 15%, 
                            rgba(34, 34, 34, 0.65) 30%,
                            rgba(34, 34, 34, 0.15) 40%,
                            rgba(34, 34, 34, 0.0)),
                          url("<%=header_large.image.attachment.url%>");
        background-size: cover;
        background-position: center 90%;
      }
    <%end%>
  </style>

  <div class="jumbo highlight">
    <div class='jumbo-message'>
      <h1>Herzlich Willkommen!</h1>
      <p>Wir laden Dich herzlich ein, Deine Kommune mitzugestalten! Hier kannst Du Dich informieren, diskutieren, deine Ideen einbringen und über Dinge abstimmen, die Dir wichtig sind.</p>
    </div>
  </div>
<% end %>
