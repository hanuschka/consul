<% if header.present? %>
  <% header_large = Widget::Card.header.find_by title: "header_large" %>

  <% if header_large %>
    <%= image_tag polymorphic_path(header_large.image.attachment), id: "welcome-header" %>
  <% end %>
<% end %>
