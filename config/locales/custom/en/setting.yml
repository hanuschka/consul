en:
  custom:
    settings:
      mailer_from_deficiency_report_address: Sender email address for deficiency reports
      mailer_from_deficiency_report_address_description: This email address will appear in emails related to deficiency reports
      ai:
        llm_provider: AI Provider
        llm_provider_description: Configure which AI service provider to use for AI features
        llm_model: AI Model
        llm_model_description: Select the AI model to use for processing requests
        llm_custom_model: Custom Model
        llm_custom_model_description: Enter a custom model name (used with Ollama or when a custom API endpoint is set)
        llm_api_endpoint: API Endpoint
        llm_api_endpoint_description: Custom API endpoint URL for OpenAI-compatible services (e.g., Azure OpenAI, proxy services)
        evaluation_context: Global AI context
        evaluation_context_description: Store background information about your municipality once, for example its name and size, the desired form of address, the names of its districts, agreed spellings and thematic priorities. The text is never shown publicly; it feeds into every AI evaluation generated on this platform afterwards. Fill it in so that AI evaluations match your municipality from the start.
        llm_api_key: API Key
        llm_api_key_description: API key for authenticating with the OpenAI service
      features:
        elasticsearch: "Elasticsearch"
        elasticsearch_description: "Elasticsearch is the leading distributed, RESTful, open source search and analytics engine designed for speed, horizontal scalability, reliability, and easy management."
