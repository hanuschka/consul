en:
  activerecord:
    errors:
      models:
        site_customization/email_template:
          attributes:
            mailer_action:
              not_registered: is not a registered email template
