en:
  components:
    adm:
      map_layers_component:
        title: Map layers
        description: Additional map layers can be included here – via a WMS service, a tile map (XYZ), or an uploaded GeoJSON file.
        table:
          name: Layer name
          type: Type
          protocol: Protocol
          layer_names: Layer name
          base: Base
          layer: Layer
        actions:
          edit: Edit
          delete: Delete
          delete_confirm: Are you sure you want to delete this map layer?
        no_layers: No map layers available.
        add_layer: Add map layer
